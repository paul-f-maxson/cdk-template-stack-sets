{
  "name": "cdk-template-stack-sets",
  "packageManager": "yarn@3.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "husky install",
    "tag-testing": "git tag --annotate --force --message 'move to $(git rev-parse --short HEAD)' testing",
    "push": "git push --follow-tags origin master",
    "pull": "git pull --prune --tags origin master",
    "stage": "yarn run tag-testing && yarn run push"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@eslint/js": "^8.38.0",
    "@types/node": "^18.15.11",
    "@typescript-eslint/eslint-plugin": "^5.58.0",
    "@typescript-eslint/parser": "^5.58.0",
    "aws-cdk": "^2.76.0",
    "eslint": "^8.38.0",
    "eslint-config-prettier": "^8.8.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.1",
    "prettier": "^2.8.7",
    "typescript": "^5.0.4",
    "typescript-eslint": "^0.0.1-alpha.0"
  },
  "resolutions": {
    "app": "portal:packages/app",
    "pipeline": "portal:packages/pipeline"
  }
}
